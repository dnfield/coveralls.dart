name: coveralls
version: 2.0.0

author: Cédric Belin <cedric@belin.io>
description: Send LCOV coverage reports to the Coveralls service.
documentation: https://cedx.github.io/coveralls.dart
homepage: https://github.com/cedx/coveralls.dart

environment:
  sdk: '>=1.24.0'

executables:
  coveralls: cli

dependencies:
  args: ^1.0.0
  crypto: ^2.0.2
  http: ^0.11.3
  lcov: ^1.0.0
  path: ^1.4.2
  which: ^0.1.3
  yaml: ^2.1.12

dev_dependencies:
  coverage: ^0.9.2
  grinder: ^0.8.0
  test: ^0.12.24
